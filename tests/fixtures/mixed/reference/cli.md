# CLI reference
| Option | Type | Description |
|---|---|---|
| `--env` | string | Target environment |

## Step 1: Install the CLI
1. Run `npm install -g app`
2. Open a new terminal
3. Enter your credentials
4. Select the default profile

```bash
app login
```
