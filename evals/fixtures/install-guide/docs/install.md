# How to install

Install from PyPI:

```bash
pip install widget
```

If your organisation mirrors PyPI, point pip at the mirror instead:

```bash
pip install --index-url https://pypi.internal/simple widget
```

Verify:

```bash
widget --version
```

If the command is not found, your PATH is missing the scripts directory that
pip reported during install.
